/** Shared `sizes` for next/image `fill` — match real layout, not default 100vw. */

/** Aircraft list slider / magnifier (≈52% column in 2-col grid, section 5vw padding). */
export const AIRCRAFT_SLIDER_SIZES = "(max-width: 768px) 90vw, 52vw";

/** Half-width column (why-us, partners row, SEO hero split, etc.). */
export const HALF_COLUMN_SIZES = "(max-width: 768px) 90vw, 50vw";

/** Quarter-width grid cell (platform cards on SEO pages). */
export const QUARTER_COLUMN_SIZES = "(max-width: 768px) 45vw, 25vw";

export const THUMB_SIZES = "84px";
export const PARTNER_LOGO_SIZES = "142px";
